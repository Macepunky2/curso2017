let miString: any = 'hola';

miString = 'hola si';

let miNumero: number = 5;

function suma(a:number , b:number) : number {
    return a + b;
}

let miArray: number [] =[ 1,2,3,];

let miArray2: Array<Number> = [1,2,3];